<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.apnatutorials.androidseekbar.MainActivity">

    <LinearLayout
        android:id="@+id/llDefault"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"

        >
        <SeekBar
            android:id="@+id/sbDefaultSeekBar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:progress="0"
            android:max="50"
            android:layout_weight="1"

            />
         <TextView
             android:id="@+id/tvDefaultProgessDetail"
             android:layout_width="20dp"
             android:layout_height="wrap_content"
             android:text="0"
             android:textStyle="bold|italic"
             android:textColor="@color/colorPrimaryDark"
            />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/llCustom"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:layout_marginTop="200dp"
        >
        <SeekBar
            android:id="@+id/sbCustomSeekBar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:progress="10"
            android:max="50"
            android:layout_weight="1"
            android:progressDrawable="@drawable/seekbar"
            android:thumb="@drawable/circle"
            />
        <TextView
            android:id="@+id/tvCustomProgessDetail"
            android:layout_width="20dp"
            android:layout_height="wrap_content"
            android:text="0"
            android:textStyle="bold|italic"
            android:textColor="@color/colorPrimaryDark"
            />

    </LinearLayout>

</RelativeLayout>
